多种数据来源，代码层可扩展，比如支持
串口，UDP通信协议读取+导出log文件
或者直接读取log/bin文件（文件地址和类型由用户通过UI选择），文件类型可扩展并且支持通信数据用户，通过UI自定义	

多路实时波形打印，支持缩放，鼠标吸附曲线点显示，以及暂停标点（对标VOFA）	

支持数据分析扩展



OK，现在开始扩展数据源为串口时的情形，数据显示，数据保存为文件，频域分析等等逻辑不变，UI添加切换数据源的选项​



串口模式下，可以选择不同的通信协议，一种是已有的和UDP一样的文本协议，还有一种是
struct
 Frame {

    
float
 fdata[CH_COUNT];

    
unsigned
 
char
 tail[4]{0x00, 0x00, 0x80, 0x7f};

};​


少了最场景的,文本协议，有一个原始数据接受区，可以直接显示文本数据，方便调试，用户可以调整编码格式UTF8 GBK 以及显示文本/十六进制


这个显示原始数据的UI，应该是通用的逻辑，不管数据源是谁，都要显示，。但是我切换到UDP的时候，显示不出原始数据，而且在我UDP连接过程中，我还能操作切换数据来源的UI，这不合理，以及串口的文本协议模式下，用户应该像再UDP模式下一样，可以设置数据头的格式​