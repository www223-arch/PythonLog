## 2026-03-17 机器学习训练按钮卡死（训练中不结束）排查记录

### 现象
- 在机器学习子窗口点击“开始训练并加载”后，按钮长期保持“训练中...”。
- 训练结果弹窗不出现，用户体感为“界面卡死”。

### 复现条件
- 入口：机器学习中心 -> 离线训练配置。
- 操作：选择训练数据与输出路径，点击训练。
- 触发点：后台线程执行训练脚本结束后，UI没有收到完成通知。

### 分析过程
1. 先核对训练线程逻辑
- 训练使用 `threading.Thread` 执行 `_train_model_worker`，线程内调用 `subprocess.run`。

2. 检查回主线程路径
- 原实现在线程内使用 `QTimer.singleShot(0, lambda: _on_train_model_finished(...))`。
- 该调用依赖调用线程事件循环；Python普通线程通常无Qt事件循环，导致回调可能不触发。

3. 结果表现与代码一致
- 回调不触发会直接导致 `train_model_busy`、按钮文本、指标文本无法复位。

### 根因结论
- 根因：跨线程回调机制不可靠，使用了依赖线程事件循环的 `QTimer.singleShot` 回调UI。
- 次因：缺少训练超时保护，异常长训练时用户无法判断是否真卡死。

### 修复方案
1. 改为Qt信号跨线程回调
- 新增 `train_finished_signal`，工作线程结束后 `emit(payload)`。
- 主线程槽函数统一调用 `_on_train_model_finished` 完成状态复位与UI更新。

2. 增加训练进度体感反馈
- 训练开始记录时间，`QTimer` 每0.5秒更新“已运行 Xs”。

3. 增加超时保护
- `subprocess.run(..., timeout=300)`。
- 超时时返回明确错误提示，避免无限等待。

### 验证结果
- 命令：`c:/Users/tk/Desktop/Pythonlog/.venv/Scripts/python.exe -m unittest tests/test_arterial_pipeline.py tests/test_regression_layering.py`
- 结果：`Ran 23 tests ... OK`。
- 静态检查：`src/app_window.py` 无错误。

### 影响范围
- 直接影响：机器学习中心离线训练流程（UI状态回调）。
- 间接影响：无（数据接收、协议解析、保存链路未改动）。

## 2026-03-17 指标PNG导出“看不到文件”排查记录

### 现象
- 勾选“保存时同步导出趋势图(PNG)”后，用户停止保存却看不到 PNG 文件路径提示。
- 用户主观感受为“没有导出PNG”。

### 复现条件
- 机器学习中心开启指标导出与PNG导出。
- 进行一段时间数据保存后点击“停止保存”。

### 分析过程
1. 检查导出触发时机
- PNG 导出在 `_stop_metrics_export()` 中执行，触发点是“停止保存/关闭窗口”。

2. 检查停止保存后的 UI 文本
- `toggle_saving()` 在调用 `_stop_metrics_export()` 后，又把导出状态文本覆盖为“指标导出文件: 未启用”。
- 导致即使导出成功，用户也立即看不到路径提示。

3. 检查失败分支可观测性
- 当保存期间无分析样本或无数值指标可绘图时，原逻辑仅返回失败，不给出明确原因。

### 根因结论
- 根因A：停止保存流程中状态文本被二次覆盖，掩盖了导出结果。
- 根因B：PNG导出失败缺少明确原因反馈，用户无法区分“未生成”与“保存失败”。

### 修复方案
1. 保留导出结果摘要
- `_stop_metrics_export()` 统一生成 `last_metrics_export_summary`，并保留在界面标签中，不再被立即重置覆盖。

2. 增强导出结果提示
- 停止保存后弹窗明确显示 CSV 路径与 PNG 路径。
- 若PNG未生成，明确提示原因（无样本、无指标、非数值等）。

### 验证结果
- 静态检查：`src/app_window.py` 无错误。
- 回归测试：`python -m unittest tests/test_arterial_pipeline.py tests/test_regression_layering.py` 通过（23项）。

### 影响范围
- 仅影响机器学习中心“指标导出”交互反馈链路。
- 不影响UDP/TCP/串口/文件接收与基础CSV保存功能。

## 2026-03-17 外部模型推理特征名告警（sklearn UserWarning）排查记录

### 现象
- 运行外部模型（如随机森林）在线推理时，控制台持续出现：
- `UserWarning: X does not have valid feature names, but RandomForestClassifier was fitted with feature names`

### 复现条件
- 训练脚本使用带列名的 DataFrame 训练模型并保存。
- 上位机在线推理阶段加载该模型并持续预测。

### 分析过程
1. 检查训练输入与推理输入类型
- 训练阶段 `tools/train_arterial_model.py` 使用 pandas DataFrame（含列名）训练。
- 推理阶段 `ModelRunner.predict()` 原实现将特征组装为裸向量（list/ndarray）调用 `predict`。

2. 对照 sklearn 行为
- 当估计器记录了训练时特征名，推理时若输入不含列名，会触发上述告警。

### 根因结论
- 根因：训练与推理输入结构不一致（训练有特征名，推理无特征名）。

### 修复方案
1. 推理输入统一为带列名 DataFrame
- `ModelRunner` 新增 `_build_model_input()`，按 `feature_order`（或排序键）构造一行 DataFrame。
- `predict/predict_proba` 统一使用 DataFrame 入参。

2. 补充回归测试
- 新增单测验证模型输入为 DataFrame 且列顺序与 `feature_order` 一致。

### 验证结果
- 静态检查：`src/analytics/ml/model_runner.py`、`tests/test_arterial_pipeline.py` 无错误。
- 回归测试：`python -m unittest tests/test_arterial_pipeline.py tests/test_regression_layering.py` 通过（24项）。

### 影响范围
- 影响范围：外部模型推理输入封装层（ModelRunner）。
- 不影响规则模式推理、数据采集链路、协议解析与保存链路。

## 2026-03-17 指标导出显示“未启用”且未产出CSV/PNG排查记录

### 现象
- 用户勾选了CSV/PNG导出，但保存时界面仍显示“指标导出文件: 未启用”，且最终无CSV/PNG文件。

### 复现条件
- 在机器学习中心勾选导出项后开始保存，或在保存中途调整导出开关。

### 分析过程
1. 导出启动时机
- 导出启动仅在“点击开始保存”时执行一次；保存中途再勾选原先不会生效。

2. 配置联动缺失
- PNG导出与CSV导出存在依赖关系（PNG依赖CSV行数据），但交互上允许只勾PNG未启用CSV，导致用户感知“勾了导出但无结果”。

3. 可观测性不足
- 启动失败时缺少即时提示，用户只看到静态“未启用”文本。

### 根因结论
- 根因A：导出配置只在保存启动瞬间读取，缺少保存过程中的动态启停能力。
- 根因B：PNG/CSV开关联动缺失，容易形成无效配置。

### 修复方案
1. 增加保存过程中的动态启停
- `metric_export_enable_checkbox` 变更时，若当前正在保存，立即启动或停止导出，并给出弹窗反馈。

2. 增加配置联动
- 勾选PNG导出时自动勾选CSV导出，避免配置矛盾。

3. 增强启动结果反馈
- `_start_metrics_export_if_needed()` 返回结构化结果（started/reason），在开始保存后立即提示失败原因。

### 验证结果
- 静态检查：`src/app_window.py` 无错误。
- 回归测试：`python -m unittest tests/test_arterial_pipeline.py tests/test_regression_layering.py` 通过（24项）。

### 影响范围
- 仅影响机器学习中心指标导出交互流程。
- 不影响主数据保存链路与分析推理结果。

## 2026-03-17 指标导出路径不可见/难定位排查记录

### 现象
- 用户勾选导出后，仍难以确认CSV/PNG到底保存到哪里，体感为“没有文件”。

### 复现条件
- 使用相对路径配置（如 `data`、`metrics_export.csv`）并从不同工作目录启动上位机。

### 分析过程
1. 路径语义检查
- 相对路径会受进程工作目录影响，用户可能在预期目录找不到文件。

2. 可观测性检查
- 原导出链路缺少生命周期级别日志（开始/写入/停止/图表导出）。
- 用户难以判断是“未启动导出”还是“导出到其他目录”。

### 根因结论
- 根因A：相对路径在不同启动方式下解析位置不一致。
- 根因B：导出过程缺少可审计日志，定位成本高。

### 修复方案
1. 导出路径绝对化
- 指标CSV/PNG路径统一解析为绝对路径（相对路径基于项目根目录）。

2. 增强调试日志
- 新增导出生命周期日志：`[EXPORT][START]`、`[EXPORT][APPEND]`、`[EXPORT][STOP]`、`[EXPORT][CHART]`。
- 能直接看到文件路径、已写行数、图表生成结果与失败原因。

### 验证结果
- 静态检查：`src/app_window.py` 无错误。
- 回归测试：`python -m unittest tests/test_arterial_pipeline.py tests/test_regression_layering.py` 通过（24项）。

### 影响范围
- 仅影响指标导出路径解析与调试可观测性。
- 不影响训练、推理、数据接收与主CSV保存。

## 2026-03-17 勾选导出但未生成CSV/PNG（未点击保存按钮）排查记录

### 现象
- 已勾选CSV/PNG导出，但运行后仍无导出文件。
- 关闭或断开时日志显示：`[EXPORT][STOP] csv=None rows=0 fields=[] chart_enabled=True`。

### 复现条件
- 连接并接收数据，但未点击“开始保存”按钮。
- 在机器学习中心勾选指标导出与PNG导出。

### 分析过程
1. 从日志判定会话状态
- 有 `STOP` 无 `START`，说明导出会话从未建立。

2. 回看启动条件
- 原逻辑把导出会话启动绑定在“开始保存”流程，未点保存时不会创建导出文件句柄。

### 根因结论
- 根因：指标导出会话错误依赖主数据保存按钮，导致用户以为“勾选即导出”但实际上未启动。

### 修复方案
1. 导出会话与保存按钮解耦
- 在 `update_data` 中增加运行时同步：连接中且勾选导出时自动启动会话。

2. 断开连接自动收尾
- 在断开流程中显式调用 `_stop_metrics_export()`，确保CSV/PNG即时落盘。

### 验证结果
- 静态检查：`src/app_window.py`、`src/core/connection_flow_mixin.py` 无错误。
- 回归测试：`python -m unittest tests/test_arterial_pipeline.py tests/test_regression_layering.py` 通过（24项）。

### 影响范围
- 影响机器学习中心指标导出触发策略。
- 不影响主CSV保存、连接状态机与数据接收路径。

## 2026-03-17 指标导出体验问题（路径显示不一致 / PNG缺失 / 日志噪声）排查记录

### 现象
- 手动选择了导出路径，但底部标签仍显示默认路径。
- 只导出了CSV，未看到PNG。
- 控制台被 FSM/UI_DEBUG 日志淹没，难以观察 Export 关键日志。

### 根因结论
- 根因A：导出会话已启动后修改路径，旧会话未重启，标签与实际路径脱节。
- 根因B：在无有效数值样本时，PNG逻辑直接跳过，用户体感为“没导出”。
- 根因C：FSM/UI调试默认开启，掩盖了Export关键信息。

### 修复方案
1. 路径变更即时生效
- CSV路径编辑后若会话在运行，立即重启导出会话并切换到新路径。
- 未运行但已启用导出时，标签先显示解析后的目标绝对路径。

2. PNG必有产物
- 无有效数值趋势时，生成带原因说明的占位PNG，避免“只有CSV无PNG”。

3. 日志降噪
- FSM/UI_DEBUG 默认关闭（可通过 `PYTHONLOG_FSM_DEBUG=1` 打开）。
- Export日志独立保留输出。

### 验证结果
- 静态检查：`src/app_window.py` 无错误。
- 回归测试：`python -m unittest tests/test_arterial_pipeline.py tests/test_regression_layering.py` 通过（24项）。

### 影响范围
- 仅影响机器学习中心导出交互与日志体验。
- 不影响采集链路、协议解析、训练与推理。

⚠️ 不足

数据处理逻辑散落在 main.py 中
没有独立的数据处理和分析模块
可视化组件单一

只有波形显示组件
缺少频域分析、统计图表等
缺少插件机制


✅ 优点
◦ 有抽象基类 DataSource，定义了统一接口
⚠️ 不足
◦ 数据处理逻辑散落在 main.py 中
◦ 没有独立的数据处理和分析模块
◦ 只有波形显示组件
◦ 缺少统计图表等
◦ 没有插件注册机制
◦ 扩展需要修改主程序

## 2026-03-14 实时性性能问题排查记录
### 现象
- 优化后最高提升到约 `710-720/s`。

1. 先排查日志输出开销
- 发现高频路径存在大量 `print`，先做降噪（保留异常日志，移除每点日志）。
1. 默认所有页面层级高于Windows导航栏。
2. 点击钉子后，该页面必须保持在所有应用之上；即使点击其他页面，其他页面也只能在其下方。

2. 检查线程与队列结构
1. 增加唯一钉住页机制
- 新增 `self._pinned_dock`，同一时刻只允许一个钉住页面。
- 新钉住时自动取消其他页面钉住状态。
- 引入后台接收线程后，确认存在队列积压与满队列丢包风险。
2. 增加强制前置保持
- 在窗口激活/点击/浮动状态变化后，统一执行 `_keep_pinned_dock_front()`，确保钉住页重新回到最上层。
- 增加状态栏性能统计，实时观察：接收速率、处理速率、队列长度、丢包数。
3. 进一步减轻闪烁
- Windows原生 `SetWindowPos` 调用移除 `SWP_SHOWWINDOW`，减少不必要的窗口重绘。

3. 检查主线程处理与 UI 刷新
- `src/main.py` 静态错误检查通过（无错误）。
- 发现每点 UI 文本更新与频繁状态切换会消耗主线程。
- 改为批处理和限频刷新，减少每点 UI 成本。
- 仅影响窗口层级策略与置顶交互，不影响数据链路和绘图逻辑。

4. 深挖串口协议解析
- 关键瓶颈：`Justfloat` 路径原先单次读取可能包含多帧，但只返回 1 帧。
- 导致吞吐被循环频率卡住。
- 改为“单次读取拆多帧入队，后续优先出队已解析帧”。

5. 增加物理链路判定指标
- 新增 `字节速率(B/s)` 与 `解析耗时(us/帧)`。
- 实测 `字节 11400-11600 B/s`，接近 `115200` 串口理论上限（约 `11520 B/s`）。

### 根因结论
- 主要瓶颈不是 UI，而是串口链路带宽上限 + 协议解析路径效率。
- 在 `115200` 下达到 `720/s` 已接近当前帧格式可达上限。
- `解析=0us/帧`（取整后）说明解析开销已不是主瓶颈。

### 已实施优化
1. 高频日志降载
- 移除数据循环中的每点日志，避免控制台 I/O 拖慢。

2. 主线程处理优化
- `update_data` 批处理、状态栏限频刷新。
- 自动保存默认不随连接自动开启，避免默认写盘影响吞吐。

3. 队列策略优化
- 满队列时“丢最旧保最新”，降低可见延迟。

4. 原始数据显示优化
- 原始数据显示改为可开关（默认关闭）。
- 改为线程安全队列 + 主线程批量刷新。

5. 串口接收解析优化
- `timeout=0` 非阻塞。
- `Justfloat` 支持单次读取多帧解析并缓存输出。
- 文本协议改为批量读取 + 行缓冲拆包，替代逐次 `readline`。

6. 热路径结构优化
- 通道去重由列表查找改为集合查找，降低高频 membership 开销。

7. 可观测性增强
- 状态栏新增：接收/s、处理/s、队列、丢包、字节B/s、解析us/帧。

### 结果
- 接收速率从约 `180/s` 提升到约 `710-720/s`。
- 在 `115200` 下，字节速率稳定在 `11400-11600 B/s`，符合物理上限预期。

### 后续建议
1. 若目标 > 1000 点/s
- 提升波特率（230400/460800/921600）。
- 或进一步减少每点字节数（优先二进制协议）。

2. 若继续追求极限实时
- 增加“性能模式”一键关闭非关键 UI（原始数据显示、吸附、频域交互）。


## 变更日志（性能专项）

### v1.4.0 - 2026-03-14（串口吞吐专项优化）

#### Added
- 新增状态栏性能指标：接收/s、处理/s、队列、丢包、字节B/s、解析us/帧。
- 新增原始数据显示开关（默认关闭，避免影响吞吐）。

#### Changed
- 原始数据处理改为线程安全队列 + 主线程批量刷新。
- 队列满时策略改为“丢最旧保最新”。
- 串口 `timeout` 调整为非阻塞读取。

#### Fixed
- 修复多处高频路径日志导致的性能抖动。

- 接收速率从约 180/s 提升到约 710-720/s。
- 在 115200 下字节速率稳定约 11400-11600 B/s，接近物理上限。
#### Notes
- 当前瓶颈主要位于串口链路带宽，不再是 UI 或解析计算。

## 2026-03-16 文件CSV导入“只有几个点/长直线”排查记录

### 现象
- 导入静态 `.csv` 后，波形看起来只有少量点。
- 中间出现长直线，视觉上像“缺点”或“跳点”。

### 分析过程
1. 先核对 CSV 解析路径与时间戳语义
- 发现 DataSaver 导出的时间戳是毫秒。
- CSV 导入路径最初按“秒”处理，进入 Manager 后又统一乘以 1000，导致时间轴尺度异常。

2. 再排查线程与队列背压
- 静态文件回放速度远高于 UI 消费速度。
- 接收线程会瞬时灌入大量帧，`data_queue` 容量过小（原 300）时容易溢出。
- 满队列策略为“丢最旧保最新”，会表现为采样点稀疏、曲线跨大段直连。

### 根因结论
- 根因A：CSV 时间戳单位不一致（毫秒/秒转换错误），引发时间轴显示异常。
- 根因B：静态文件回放无节流 + 队列容量偏小，引发丢帧，波形被长线连接。

### 修复方案
1. 统一 CSV 时间戳单位
- 在文件源 CSV 解析处把 CSV 时间戳按毫秒读取，再换算成秒返回给 Manager。
- 保持 Manager 的统一语义（上游秒、下游毫秒）不变。

2. 降低静态文件回放丢帧概率
- 接收线程对文件源增加轻微节流（每帧约 0.5ms）。
- `data_queue` 容量从 300 提升到 5000。

3. 测试补齐
- 新增 CSV 时间戳尺度回归测试。
- 新增 CSV 多行全量读取测试（100 行必须读满）。

### 验证结果
- 回归测试通过（16 项）。
- 现场复测反馈：问题已解决。

### 其他格式是否存在同类问题（影响分析）
1. 文件源 `.log/.bin`
- 结论：存在同类风险（中高概率），机制与本次 CSV 一致。
- 原因：同属“静态文件回放”路径，生产速度可能远高于 UI 消费速度，队列满时会丢最旧帧，表现为点稀疏、长线连接。
- 当前缓解：本次在接收线程对文件源统一加轻微节流，并提升 `data_queue` 容量，因此 `.log/.bin/.csv` 已同步受益。
 
2. UDP/TCP/串口（实时链路）
- 结论：存在相似现象的可能（中低概率），但触发机制不同。
- 原因：若输入频率持续超过链路+解析+UI总吞吐，队列同样可能溢出并触发丢帧策略，可能看到“跨点连线”。
- 差异：实时链路通常受物理链路/发送速率约束，不像静态文件可瞬时灌入大量历史数据。

3. 时间戳单位类问题
- 结论：当前仅在 CSV 导入链路确认过该风险并已修复。
- 原因：`.log` 文本协议与 `.bin justfloat` 时间语义与 CSV 导出格式不同，不共享 CSV 的“毫秒列再换算”路径。
- 建议：后续若新增文件协议，必须在协议定义中显式声明时间戳单位，并补对应回归测试。

4. 后续监控建议
- 观察状态栏 `队列` 与 `丢包` 指标；若长时间增长，优先判定为背压问题。
- 对文件回放场景，必要时增加“按时间戳节奏回放”模式，进一步降低视觉稀疏感。

### 经验沉淀
- 文件回放链路与实时链路在“生产/消费速率比”上差异明显，必须单独考虑背压策略。
- 任何涉及时间戳的协议扩展都必须明确单位并在测试中锁定。

## 2026-03-16 UDP发送5000Hz时上位机卡顿分析

### 现象
- 将 `tools/udp_sender.py` 的发送频率提升到 `5000Hz` 后，上位机出现明显卡顿。
- 体感表现为：UI响应变慢、波形刷新不流畅，可能伴随丢包增加。

### 复现条件
- 数据源：UDP 文本协议。
- 发送速率：`5000` 点/秒（3通道时约 `15000` 通道值/秒）。
- 接收端：默认实时绘图与状态刷新逻辑开启。

### 分析过程
1. 发包路径检查
- `udp_sender` 正弦发送循环按 `sample_rate` 控制 `sleep` 间隔，速率提升会直接放大接收与解析压力。
- 发送器参数中存在 `--rate`，但 `send_sine_wave` 内历史实现使用固定 `sample_rate`（工具侧存在速率控制一致性风险）。

2. 协议解析路径检查（UDP Source）
- 每个 UDP 包会走文本解析：`split(',') + float()`。
- 解析后 `get_channel_names()` 仍基于 `last_raw_text` 再次 `split(',')` 提取通道名，存在重复字符串解析开销。

3. 应用层消费能力检查（Main/UI）
- 主线程 `update_data` 每 5ms 触发，每轮上限 `500` 帧，理论上限足够高。
- 但高频下瓶颈更多体现在：Python 文本解析成本 + 高频队列搬运 + Qt/pyqtgraph 重绘开销叠加。
- 波形组件以 `20ms` 刷新一次显示，输入过高时主线程在“处理数据+渲染”之间竞争，产生卡顿体感。

### 根因结论
- 根因A：UDP 文本协议在 `5000Hz` 下解析成本显著上升（字符串切分+浮点转换）。
- 根因B：通道名提取存在重复解析，放大 CPU 消耗。
- 根因C：高频数据处理与绘图刷新叠加，主线程时间片不足，导致 UI 卡顿。

### 影响范围
- UDP 文本协议：高风险（本次直接命中）。
- TCP 文本协议：中高风险（解析路径类似，速率足够高时可复现同类问题）。
- 串口文本协议：中风险（受物理带宽限制，较难到 5000Hz，但机制相同）。
- 二进制协议（如 justfloat）：相对低风险（解析开销更低），但极限速率下仍可能受 UI 渲染瓶颈影响。

### 优化建议（按优先级）
1. 协议优化优先
- 高速场景优先使用二进制协议（justfloat）替代文本协议。

2. 解析路径降耗
- UDPSource 在 `_parse_text_data` 阶段一次性缓存通道名，`get_channel_names()` 直接返回缓存，避免重复 `split`。

3. 可视化限载
- 高速模式下提高绘图刷新间隔（如 20ms -> 30/40ms）或降低每轮绘图复杂度。

4. 发送工具一致性
- 统一 `udp_sender` 的 `--rate` 参数与实际发送速率实现，避免测试速率与预期不一致。

### 验证建议
- 观察状态栏 `接收/s、处理/s、队列、丢包` 四项联动。
- 若 `队列` 持续上升且 `丢包` 增加，优先判定为吞吐超过当前实时渲染能力。

## 2026-03-16 连接时左侧控制面板宽度跳变与布局体验优化

### 现象
- 点击连接/切换协议时，左侧控制面板宽度偶发“跳变”，视觉卡顿明显。
- 用户希望：
	1. 面板尺寸可调且可锁定；
	2. 每个大块支持拖拽分离；
	3. 支持一键恢复默认布局与尺寸。

### 分析过程
1. 原布局结构
- 主界面使用 `QHBoxLayout` 固定比例（左1:右3）+ 右侧 `QSplitter`。
- 左侧控制面板内多个组在连接过程中会显隐与内容变化，布局管理器在比例约束下会反复重算宽度。

2. 跳变触发机制
- 控制面板内容高度/最小宽度变化（例如不同协议组显隐）会触发主布局重新分配宽度。
- 由于不是用户显式拖拽，而是布局重算，体感为“卡一下、宽度突然变”。

3. 方案比较
- 仅加 `QSplitter`：可调但不能自然支持“分离面板”。
- 改为 `QDockWidget`：天然支持拖拽重排、浮动分离、状态保存/恢复，更符合需求。

### 根因结论
- 根因：左侧面板处于比例布局中，且内部分组动态显隐导致宽度约束变化，触发非用户预期的重布局跳变。

### 修复方案
1. 布局架构改造
- 将三大块改为 Dock 面板：控制面板、波形区、原始数据与发送区。
- 支持拖拽重排、独立浮动分离。

2. 尺寸锁定
- 新增“锁定布局”动作：按当前尺寸冻结各 Dock（禁止拖拽分离，并固定当前宽/高）。

3. 一键复原
- 保存默认 `saveGeometry/saveState`，新增“一键复原布局”动作，恢复初始布局与尺寸。

4. UI观感优化
- 新增布局工具栏与 Dock 标题样式（渐变与圆角按钮），避免“纯文本+生硬按钮”观感。

### 验证结果
- 代码静态检查通过（`src/main.py` 无语法错误）。
- 交互层面可完成：拖拽分离、尺寸锁定、一键恢复。

### 影响范围
- 仅涉及 UI 布局与交互层，不改变数据源解析、线程队列与协议行为。

## 2026-03-17 点阵压力UDP发送后上位机收不到数据

### 现象
- 使用 `python tools/pressure_matrix_sender.py --mode udp --grid-width 16 --grid-height 16 --rate 30 --duration 30` 发送时，上位机无曲线、压力平面无更新。

### 复现条件
1. 上位机数据源选择 UDP，监听端口 8888。
2. 启动点阵发送器，发送 16x16 文本点阵帧（`p_row_col=value`）。
3. 上位机持续显示“无数据/等待数据”。

### 分析过程
1. 检查协议格式
- 发送端帧格式正确：`DATA,timestamp,p_0_0=...,p_0_1=...,...`。
- 数据头与上位机默认校验头一致（`DATA`）。

2. 检查接收实现
- `src/data_sources/udp_source.py` 默认 `buffer_size = 1024`。
- 16x16 点阵文本帧通常远超 1KB，`recvfrom(1024)` 会截断 UDP 数据报。

3. 截断后果
- 被截断的文本尾部不完整，解析时出现字段缺失/浮点转换异常。
- 解析返回空元组，Manager 将其视作格式错误或无效帧，最终 UI 无有效更新。

### 根因结论
- 根因：UDP 接收缓存默认值过小（1KB），无法承载点阵压力文本大包，导致报文截断与解析失败。

### 修复方案
1. 调整默认接收缓冲
- `UDPDataSource.buffer_size` 从 `1024` 提升到 `65535`（UDP 单报文上限）。

2. 提升系统 socket 接收缓冲
- 在 `connect()` 中设置 `SO_RCVBUF` 为更高值（`max(262144, buffer_size*4)`），降低高频场景丢包风险。

3. 回归测试补齐
- 新增测试：`tests/test_regression_layering.py::test_udp_default_buffer_is_large_enough_for_matrix_frames`，防止默认值回退。

### 验证结果
- 自动化回归通过（含新增测试）。
- 在 16x16 点阵发送场景下，报文不再因 1KB 缓冲被截断。

### 影响范围
- 影响模块：`src/data_sources/udp_source.py`。
- 风险评估：低。仅扩容接收缓冲，不改变协议语义与上层接口。
- 受益链路：UDP 文本大包场景（尤其点阵压力、通道数较多场景）。

## 2026-03-16 Dock拖拽后页面“被吞”仅能复原找回

### 现象
- 三页面模式下，先把一个页面拖回主区，再拖动另一个页面时，自动布局偶发把某页挤没。
- 用户体感为“页面消失”，只能点“复原布局”才能看到。

### 分析过程
1. 合并页可见性检查
- 发现 Dock 标题被统一设置为空白，页面合并成标签后，标签文本不可辨识，用户难以切换到“被遮住”的页。

2. 自动布局塌陷检查
- 在频繁拖拽和区域重算后，个别 Dock 可能被压缩到极小尺寸（接近不可操作），视觉上等价于“吞页”。

### 根因结论
- 根因A：合并标签缺乏可识别标题，导致页面虽在但难以切回。
- 根因B：布局重算时缺少最小可操作尺寸与塌陷后的自动重平衡。

### 修复方案
1. 恢复可识别Dock标题
- 三个页面标题改为明确名称（控制面板/波形页面/原始数据发送），保证合并后标签可切换。

2. 增加防塌陷保护
- Dock 创建时设置最小尺寸下限（220x140）。
- 布局变化后触发轻量重平衡：若检测到停靠页宽高过小，自动按默认比例恢复可操作尺寸。

3. 增加快速恢复入口
- 工具栏新增“显示全部页面”，用于快速拉回可见状态，无需整页复原。

### 验证结果
- `src/main.py` 语法检查通过（无错误）。
- 从交互路径上覆盖：合并标签可见、塌陷自动回弹、可一键显示全部页面。

### 影响范围
- 仅影响布局交互层，不涉及数据采集、协议解析与保存链路。

## 2026-03-16 Dock页面显示澄清：非吞页，仅不需要独立标题栏

### 现象
- 三页面拖拽后底部仍有可切换小标签页，页面并未真实丢失。
- 用户诉求是去掉三个Dock独立标题栏（控制面板/波形页面/原始数据发送），保留标签切换即可。

### 分析结论
- 前一版修复中保留了Dock标题文本用于标签可识别，但在非合并状态下仍会显示独立标题栏。
- 这与当前交互预期不一致：用户不希望看到独立标题栏。

### 修复方案
1. 标题栏策略调整
- 停靠状态：统一隐藏Dock独立标题栏。
- 浮动状态：恢复系统标题栏，保证浮动窗口可操作性。

### 验证结果
- `src/main.py` 语法检查通过（无错误）。
- 停靠布局下不再显示三个独立标题栏。

### 影响范围
- 仅影响Dock外观与交互，不影响数据链路与采集性能。

## 2026-03-16 Dock文字UI冗余精简

### 现象
- 用户不需要三个Dock文字标签（控制面板/波形页面/原始数据发送），认为视觉冗余。

### 分析结论
- 在已隐藏停靠标题栏后，Dock标题文本仍会用于标签文字显示。
- 对当前使用习惯而言，该文字信息价值低于界面简洁性。

### 修复方案
1. 将三个Dock的显示标题设为空字符串。
2. 保留Dock对象名与布局行为，不影响拖拽、分离、复原、显示全部页面。

### 验证结果
- `src/main.py` 语法检查通过（无错误）。

### 影响范围
- 仅影响界面文字展示，不影响功能行为。

## 2026-03-16 Dock标题栏显示策略回调

### 现象
- 用户希望保留Dock标题栏框体，不要隐藏标题栏。
- 同时维持“去掉三个冗余文字标题”的简洁视觉。

### 修复方案
1. 调整标题栏策略为始终使用系统标题栏（不在停靠态隐藏）。
2. 三个Dock标题继续为空字符串，仅保留标题栏框体，不显示冗余文字。

### 验证结果
- `src/main.py` 语法检查通过（无错误）。

### 影响范围
- 仅影响Dock外观策略，不影响交互与数据链路。

## 2026-03-16 布局工具栏去文字化与功能精简

### 需求
- 移除“显示全部布局”按钮及功能。
- “锁定布局/复原布局”改为形象图标UI，不显示文字。
- 鼠标悬停时显示简短提示。

### 修复方案
1. 工具栏精简
- 删除“显示全部页面”动作及对应方法。

2. 图标化交互
- `锁定布局` 改为可切换图标（锁定/解锁两态）。
- `复原布局` 改为回环箭头图标。
- 工具栏强制 `IconOnly` 模式，仅显示图标按钮。

3. 提示文案
- 为两个图标动作增加 `ToolTip/StatusTip`：
	- 锁定按钮：锁定布局 / 解锁布局（随状态切换）
	- 复原按钮：复原布局

### 验证结果
- `src/main.py` 语法检查通过（无错误）。

### 影响范围
- 仅影响布局工具栏外观与交互入口，不影响数据采集与显示链路。

## 2026-03-16 锁定图标语义优化与按钮再缩小

### 需求
- 锁定按钮改为同一把锁，点击后在“上锁/解锁”两态之间切换。
- 锁定与复原两个图标按钮再小一点。

### 修复方案
1. 锁图标重绘
- 闭锁状态绘制闭合锁梁。
- 解锁状态绘制打开锁梁，视觉上明确区分开/关。

2. 按钮与图标缩小
- 工具栏图标尺寸从 `18x18` 调整为 `14x14`。
- 按钮最小尺寸从 `28x28` 调整为 `22x22`，内边距由 `4px` 调整为 `2px`。

### 验证结果
- `src/main.py` 语法检查通过（无错误）。

### 影响范围
- 仅影响布局工具栏视觉表现，不影响布局锁定/复原功能语义。

## 2026-03-16 工具栏图标绘制导致启动崩溃

### 现象
- 启动主程序时在 MainWindow 初始化阶段崩溃。
- 报错为 QPainter.drawLine 参数重载不匹配（传入 float 四参）。

### 根因
- 在锁图标开锁状态绘制中，调用 `drawLine(x1, y1, x2, y2)` 时使用了浮点数。
- PyQt5 该重载仅接受 int；若使用浮点应走 QPointF/QLineF 重载。

### 修复方案
1. 将该调用改为 QPointF 版本：
- `painter.drawLine(QPointF(...), QPointF(...))`

### 验证结果
- `src/main.py` 静态错误检查通过（无错误）。

### 影响范围
- 仅影响图标绘制实现与启动稳定性，不影响业务数据链路。

## 2026-03-16 浮动页右上角“返回/置顶”控制

### 需求
- 每个被拖出来（浮动）的页面右上角显示两个小按钮：
	- 返回：将当前分离页面放回主布局
	- 置顶：使该浮动页始终显示在所有应用之上

### 修复方案
1. 为每个Dock创建浮动控制小面板
- 新增右上角 `返回/置顶` 两按钮，仅在浮动状态可见。

2. 返回行为
- 点击“返回”后取消浮动并回到最近停靠区域。

3. 置顶行为
- 通过 `WindowStaysOnTopHint` 切换单个浮动页置顶状态。

4. 稳定性修复
- 修复 `dockLocationChanged` 回调参数不匹配风险，支持记录最近停靠区域用于“返回”。

### 验证结果
- `src/main.py` 静态错误检查通过（无错误）。

### 影响范围
- 仅影响浮动页窗口交互，不影响数据采集与绘图链路。

## 2026-03-16 浮动页置顶与最小化联动问题（整合记录）

### 目标
- 浮动子页面支持“钉住置顶”，且应高于其他子页面与外部应用窗口。
- 浮动子页面不跟随主窗口最小化。
- 主窗口关闭时，子页面随进程正常关闭。

### 过程与关键结论
1. 基础能力建设
- 将置顶按钮改为图钉图标，补齐开关状态提示。
- 新增单一钉住页机制：`self._pinned_dock`，同一时刻只允许一个钉住页。

2. 置顶策略对齐
- 参考 `src/test.py`：使用 `setWindowFlags(Qt.Window | Qt.WindowStaysOnTopHint)`，并在切换后 `show()` 生效。
- 同时保留 Win32 `SetWindowPos` 作为增强层（Qt + Win32 双层策略）。

3. 回归故障修复
- 出现 `RecursionError`（`visibilityChanged -> _on_dock_visibility_changed`）后，定位到可见性回调重入与 `SWP_SHOWWINDOW` 触发抖动。
- 修复：增加可见性防重入锁，移除 `SWP_SHOWWINDOW`，减少无效 `setWindowFlags` 调用。

4. 窗口关系定位
- 诊断结论：问题不在“Dock 天生不可置顶”，而在浮动 Dock 与主窗口的 owner/transient 关系及后续状态覆盖。
- 增加 transient/owner 管理与 `WIN_DEBUG` 诊断链路（按需通过 `PYTHONLOG_WIN_DEBUG=1` 开启）。

5. 稳定性收敛
- 根据日志将守护频率从 `300ms` 调整为 `1000ms`，避免高频重排造成抖动与主线程压力。
- 守护范围收敛为“主窗口 + 钉住页”，并将钉子语义收敛为 `should_top = on_top`。
- Win32 置顶采用“`HWND_NOTOPMOST -> HWND_TOPMOST`”提升重排成功率。

6. 最终关键修复
- 修复“钉住状态被瞬时 `visible=False` 误清空”的问题。
- 仅在真正回停靠（`isFloating=False`）时清空 `_pinned_dock`。
- 对钉住页持续执行 `setTransientParent(None)`、`set_owner(None)`、Qt 置顶标志与 Win32 置顶重申。

### 最终实现要点
- `_pinned_dock` 唯一钉住页状态管理。
- `_set_floating_dock_on_top`：钉子开关控制置顶语义。
- `_keep_pinned_dock_front`：钉住页持续前置与置顶重申。
- `_on_dock_visibility_changed`：可见性补偿与防重入。
- `_on_dock_top_level_changed` / `changeEvent`：浮动态与主窗口状态联动修正。

### 验证与影响
- `src/main.py` 静态错误检查通过（无错误）。
- 影响范围仅限 Dock 浮动窗口层级与置顶行为，不影响数据采集、解析与波形绘图链路。

## 2026-03-16 波形窗口“跟随最新 + 最佳缩放”功能

### 需求
- 曲线打印窗口应跟着新数据走。
- 在跟随最新数据时，支持对“最新一段数据”保持最佳缩放。

### 实现
1. 新增时域图形UI控制
- 新增 `跟随最新` 开关。
- 新增 `最新窗口最佳缩放` 开关。
- 新增 `窗口点数`（用于定义“最新一段数据”长度）。

2. 新增视图同步逻辑
- 在 `update_display` 后执行视图同步。
- `最佳缩放` 开启时：对最新窗口数据自动计算 X/Y 范围并更新视图。
- 仅 `跟随最新` 开启时：保持当前缩放跨度，仅平移X轴到最新数据。

3. 参数接口
- 新增 `set_follow_latest`、`set_best_fit_latest`、`set_follow_window_points`。

### 验证
- `src/visualization/waveform_widget.py` 静态错误检查通过（无错误）。
- `src/main.py` 静态错误检查通过（无错误）。

### 影响范围
- 仅影响波形时域显示交互，不影响数据接收、解析和存储链路。

## 2026-03-16 跟随模式交互调整（移除持续最佳缩放）

### 需求
- 删除“保持最佳缩放”功能。
- 每次开启“跟随最新”时自动调整一次视图，后续由用户自由缩放。

### 调整内容
1. UI收敛
- 移除“最佳缩放”独立开关按钮，仅保留“跟随最新(L)”与窗口点数。

2. 行为调整
- 开启“跟随最新”时触发一次性自动调整（按最新窗口数据设置X/Y范围）。
- 一次自动调整后不再自动改Y轴；用户可自由手动缩放。
- 后续仅保持X轴跟随最新数据平移。

### 验证
- `src/visualization/waveform_widget.py` 静态错误检查通过（无错误）。

### 影响范围
- 仅影响时域视图的跟随与缩放交互，不影响采集、解析、保存链路。

## 2026-03-16 跟随UI精简与置顶页防自动回停靠

### 需求
- 移除时域跟随控件中的 `N` 与数字输入。
- “开启跟随”UI更形象。
- 子页面置顶后保持浮动在主页面上方，不自动回到主页面；取消置顶维持现有逻辑。

### 调整内容
1. 跟随UI图形化
- 跟随开关由字母按钮改为图标按钮（前进/跟随语义）。
- 保留小尺寸与悬浮提示，移除 `N` 和窗口点数输入控件。

2. 置顶防回停靠
- 置顶路径对 `QDockWidget` 跳过 `setWindowFlags` 方案，避免触发窗口关系重建导致自动回停靠。
- 置顶维持通过 native topmost + owner/transient 维持。

### 验证
- `src/visualization/waveform_widget.py` 静态检查通过（无错误）。
- `src/main.py` 静态检查通过（无错误）。

### 影响范围
- 仅影响跟随UI外观与Dock置顶行为，不影响数据采集、解析、存储和绘图链路。

## 2026-03-16 置顶页自动回停靠与置顶失效修复

### 现象
- 子页面置顶后仍会自动调整布局并回到主页面停靠区。
- 回停靠后置顶效果失效，无法持续保持在所有页面上方。

### 根因
- 在Dock状态变化过程中，置顶页面可能被短暂切回 `isFloating=False`。
- 旧逻辑在该状态下未做强制恢复，导致页面被保留在主布局并失去置顶语义。

### 修复方案
1. 增加钉住页防回停靠机制
- 新增 `_force_refloat_pinned_dock`，检测到钉住页被挤回停靠态时，下一事件循环强制恢复浮动。

2. 状态变化回调中自动纠偏
- 在 `_on_dock_top_level_changed` 中，若目标为钉住页且变为非浮动，立即调度强制恢复逻辑。

3. 避免误清空置顶状态
- 在浮动控件可见性路径中，若页面仍属于 `_pinned_dock`，不因瞬时非浮动状态清空 `_is_on_top`。

### 验证
- `src/main.py` 静态检查通过（无错误）。

### 影响范围
- 仅影响置顶浮动页的停靠状态维持与层级行为，不影响数据采集和波形链路。

## 2026-03-16 置顶回归修复（修复前次改动引入）

### 现象
- 置顶功能较此前版本弱化，表现为置顶失效。
- 置顶页仍有概率自动回停靠。

### 根因
- 在 `topLevelChanged` 浮动态路径中调用 `dock.setWindowFlag(Qt.Window, True)`，会触发Dock窗口关系重建，导致置顶与浮动状态不稳定。
- 置顶守护频率过低时，跨应用切换后恢复不够及时。

### 修复
1. 移除高风险路径
- 浮动态不再执行 `dock.setWindowFlag(Qt.Window, True)`，改为仅维护 owner/transient。

2. 恢复置顶即时性
- 置顶守护定时器从 `1000ms` 调整为 `300ms`。

3. 置顶时强制重申窗口关系
- 在钉子切换置顶时，若页面为浮动态，先重申 `transient=None` 与 `owner=None`，再走 native topmost。

### 验证
- `src/main.py` 静态检查通过（无错误）。

### 影响范围
- 仅影响浮动页置顶与防回停靠行为，不影响数据采集、解析与绘图链路。

## 2026-03-16 参考历史方案的置顶增强回补

### 触发
- 用户反馈：置顶子页面仍会被其他应用遮挡。

### 参考来源
- 从历史记录中复用“Windows原生置顶增强”路径：
	- 先 `HWND_NOTOPMOST` 再 `HWND_TOPMOST`。
	- 对置顶动作使用显示/前置增强，提升跨应用层级抢占成功率。

### 本次回补
1. 调整 Win32 置顶 flags
- `on_top=True` 时使用 `SWP_SHOWWINDOW`（保证窗口处于可见前置路径）。
- `on_top=False` 保持 `SWP_NOACTIVATE` 的非激活降级。

2. 增强前置
- 置顶后尝试 `SetForegroundWindow(hwnd)`，提高被其他应用遮挡场景下的恢复成功率。

### 验证
- `src/main.py` 静态检查通过（无错误）。

### 影响范围
- 仅影响Windows下置顶抢占行为，不影响数据采集、解析与绘图逻辑。

## 2026-03-16 钉住状态误清空排查结论

### 结论
- 该原因成立：被钉住页在状态抖动（瞬时不可见/瞬时非浮动）期间，存在被误清空 `_pinned_dock` 的风险。
- 一旦 `_pinned_dock` 被清空，后续守护链路无法持续重申 owner/transient/topmost，表现为“只剩主窗口在顶层”。

### 修复
1. 移除误清空路径
- 在 `_update_floating_controls_visibility` 中不再清空 `_pinned_dock`。

2. 清空职责收敛
- 仅在“主动取消置顶”或“主动返回停靠”时清空 `_pinned_dock`。

### 验证
- `src/main.py` 静态检查通过（无错误）。

### 影响范围
- 仅影响置顶状态机一致性与守护连续性，不影响数据采集和绘图链路。

## 2026-03-16 参考历史方案继续加固（QWindow层置顶重申）

### 触发
- 用户反馈：钉住页仍可能被外部应用遮挡。

### 分析
- 仅依赖 Win32 `SetWindowPos` 在部分切换场景下存在恢复不及时。
- `QDockWidget` 不适合反复 `QWidget.setWindowFlags`，否则可能引发布局/浮动态副作用。

### 加固方案
1. 新增 QWindow 层置顶重申
- 对浮动Dock使用 `windowHandle().setFlag(Qt.WindowStaysOnTopHint, on_top)`。
- 在钉住守护与钉子切换时重申，避免仅靠 Win32 单通道。

2. 守护节奏调整
- 高频守护优先钉住页（持续重申 owner/transient/topmost）。
- 主窗口改为低频重申，减少全局重排对钉住页的干扰。

### 验证
- `src/main.py` 静态检查通过（无错误）。

### 影响范围
- 仅影响钉住页跨应用层级维持策略，不影响数据采集、解析与绘图链路。

## 2026-03-16 钉住页“先停靠再弹回”体验修复

### 现象
- 钉住子页面放在主页面上方时，会先触发停靠回主布局，再被逻辑弹回浮动。
- 虽然功能最终可用，但视觉抖动明显，体验差。

### 根因
- 旧方案依赖“触发停靠后再强制回浮动”的纠偏路径。
- 用户拖动钉住页经过主窗口可停靠区域时，Qt仍会尝试停靠，产生回弹闪动。

### 修复方案
1. 钉住即禁止停靠
- 新增 `_lock_pinned_dock_docking`：钉住后将 `allowedAreas` 设为 `Qt.NoDockWidgetArea`。
- 保持 `DockWidgetMovable | DockWidgetFloatable`，页面仍可自由拖动浮动。

2. 取消钉住恢复原能力
- 新增 `_unlock_pinned_dock_docking`：取消钉住或返回主布局时恢复原 `allowedAreas/features`。

3. 与现有流程衔接
- 钉住开关打开时锁定停靠；关闭时恢复停靠。
- “返回主布局”前先解除锁定，避免状态残留。

### 验证
- `src/main.py` 静态检查通过（无错误）。

### 影响范围
- 仅影响钉住页的拖拽/停靠交互体验，不影响置顶语义与数据链路。
## 2026-03-16 置顶页闪烁修复（强置顶与守护置顶分离）

### 现象
- 钉住子页面放在主页面上方时出现明显闪烁。

### 根因
- 高频守护路径反复使用强置顶（显示/前置）会触发重绘与焦点扰动。

### 修复方案
1. 置顶分级
- 新增 aggressive 模式参数：
- aggressive=True 仅用于用户点击钉子置顶、强制回浮动等关键时机。
- aggressive=False 用于周期守护，采用无激活温和置顶重申。

2. 守护去闪烁
- 周期守护改为温和置顶，避免持续 SHOWWINDOW/前置。

### 验证
- src/main.py 静态检查通过（无错误）。

### 影响范围
- 仅影响窗口层级重申策略与视觉稳定性，不影响采集、解析、绘图和保存链路。
